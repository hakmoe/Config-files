# Config-files
Config files, mainly for bash. To create it make a git repo, for example:

```console
mkdir ~/.dotfiles
git clone github.com/hakmoe/Config-files/ ~/.dotfiles
```
then make symbolic link to the .bashrc in .dotfiles to the home directory
```console
ls -s ~/.dotfiles/.bashrc ~/.bashrc
```

